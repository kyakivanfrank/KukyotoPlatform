import React from 'react';

// import NameItem  from './NameItem';

export default ({ filteredTotemsData})=> {

  return filteredTotemsData.map( nem => ( <div>these are the names they need to be finished</div>
      
    // <NameItem key={ nem.index }  nem={nem}/>
    // the next part is to try connecting it to other names
    
    ));
    
  }
  

 